server:
  port: 84

spring:
  application:
    name: nacos-order-consumer
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

#激活sentinel对feign的支持
feign:
  sentinel:
    enabled: true